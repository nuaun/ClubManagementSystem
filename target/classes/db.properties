# Template — committed to repo. DO NOT put real credentials here.
# Real credentials go in db.properties.local (gitignored).
#
# BUG-6 FIX: Added allowPublicKeyRetrieval=true for MySQL 8 compatibility.
# Setup steps:
#   1. CREATE DATABASE isc_ms CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
#   2. Create db.properties.local with your real credentials (see db.properties.local as example).
#   3. Run schema SQL to create tables.
db.url=jdbc:mysql://localhost:3306/isc_ms?useSSL=false&serverTimezone=Europe/Istanbul&allowPublicKeyRetrieval=true
db.user=YOUR_DB_USER
db.password=YOUR_DB_PASSWORD
